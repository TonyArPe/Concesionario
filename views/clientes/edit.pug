extends ../templates/layout

block content 

    form(method="post") 
        .campo
            label.form-label(for="Marca") Marca:
            input.form-control(type="text", name="nombre" value=cliente.Nombre)
        .campo
            label.form-label(for="Modelo") Modelo:
            input.form-control(type="text", name="telefono" value=cliente.Telefono)
        .campo
            label.form-label(for="Anio") Año:
            input.form-control(type="text", name="direccion" value=cliente.Direccion)
        input(type="hidden", name="id", value=cliente.ID_Cliente)
        button.edit(type="submit") Editar
