body
        
    header
        if currentUser
            img(src="/images/logo1.png" alt="Logo concesionario")         
            a(href="/vehiculos") VEHÍCULOS
            a(href="/clientes") CLIENTES
            a(href="/compras") COMPRAS
            a(href="/ventas") VENTAS
            a.salir(href="/auth/logout")
                i.material-icons logout
        if !currentUser
            img.no-session(src="/images/logo1.png" alt="Logo concesionario")    