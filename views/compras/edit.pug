extends ../templates/layout

block content 

    form(method="post") 
        .campo
            label(for="Precio") Precio:
            input(type="text", name="Precio_Compra" value=compra.Precio_Compra)
        .campo
            label(for="Vehiculo") Vehiculo:
            input(type="text", name="ID_Vehiculo" value=compra.ID_Vehiculo, disabled)
        input(type="hidden", name="id", value=compra.ID_Compra)
        
        button.edit(type="submit") Editar
